#pragma once

namespace Texture
{
	unsigned int generate16bitTexture(unsigned int, unsigned int);
	unsigned int generateEmpty16bitTexture(unsigned int, unsigned int);
	unsigned int generateEmpty16MSAAbitTexture(unsigned int w, unsigned int h);
	unsigned int generate8bitTexture(unsigned int, unsigned int);
}
